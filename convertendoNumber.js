let largura = "10";
let altura = "5";
// podemos usar o operador de soma + para fazer a conversao de texto para numero 

console.log(+largura*+altura);
let meuNumero = "leonardo";
console.log(Number(meuNumero));
console.log(+meuNumero);

let usuarioConectado = false;

//conversao de booleano para number
console.log(Number(usuarioConectado));
usuarioConectado=true;
console.log(Number(usuarioConectado))
